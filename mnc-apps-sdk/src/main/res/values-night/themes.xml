<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="PrimaryText" parent="android:TextAppearance">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="SecondaryText" parent="android:TextAppearance">
        <item name="android:textColor">@color/white</item>
    </style>
</resources>